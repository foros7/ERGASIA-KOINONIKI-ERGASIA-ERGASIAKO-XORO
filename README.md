# Simple React Site

Minimal Vite + React template.

Prerequisites:

- Node.js (16+ recommended)

Install and run:

```powershell
npm install
npm run dev
```

Build and preview:

```powershell
npm run build
npm run preview
```
